---
description: Research integrity — no fabricated results
globs: research/**,reports/**
alwaysApply: false
---

# Research integrity

- Never hardcode experiment metrics in `research/` or `reports/`.
- Mark uncertain claims with `[uncertain]`.
- Claims must link to `experiment_id` in `runtime/state/experiment_provenance.yaml` when stating results.
