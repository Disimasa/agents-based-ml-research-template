# Literature

User-facing literature artifacts per topic.

| Path | Purpose |
|------|---------|
| `{topic}/results/*.json` | Verified source cards (title, DOI, extracted fields) |
| `{topic}/README.md` | Optional summary — gaps, trends, reading guide |

Agent working files (`outline.yaml`, `fields.yaml`) live in `runtime/state/literature/{topic}/`.
